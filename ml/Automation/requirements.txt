polars==0.20.25; platform_system != 'Windows'
pandas==2.1.3; platform_system == 'Windows'
numpy==1.24.3
requests==2.31.0
# Optional for Bolt directly to Memgraph (skip if using HTTP proxy)
# mgclient==1.4.0
# Optional GPU libs
# torch==2.1.0
